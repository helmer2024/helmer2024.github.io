const createDesignInCanva = async (designContent) => {
  console.log('Simulando la creación de diseño en Canva:', designContent);
  // Aquí iría el código real que haría la solicitud a Canva cuando tengas la clave API.
};
